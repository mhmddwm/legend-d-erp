# Scripts

## Windows
- `start-backend.bat`: starts FastAPI backend on port 8080.
- `start-frontend-static.bat`: serves current static frontend on port 5500.

Recommended for now: use backend if you want API + frontend together, or Live Server for frontend-only checks.
