# Cloudflare Pages Deployment

For the current static frontend:
- Build command: empty
- Output directory: `frontend`

When the frontend becomes React/Vite later, update the build command and output directory accordingly.
